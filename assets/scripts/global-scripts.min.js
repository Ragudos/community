function removeError(input) {
    const errorId = input.getAttribute("aria-errormessage");
    if (errorId) {
        const errorEl = document.getElementById(errorId);
        if (errorEl) {
            errorEl.innerHTML = "";
            errorEl.removeAttribute("data-errored");
        }
    }
}