<span class="title-sm">Sign in to your account.</span>
<form hx-swap="innerHTML" hx-replace-url="/discover" data-loading-delay="50" data-loading-aria-busy
    hx-target="body" class="auth-form" data-loading-path="/auth/api/login" hx-target-error="#nothing" hx-sync="#submit-form, a"
    hx-post="/auth/api/login" hx-trigger="submit" method="post" action="/auth/api/login" id="submit-form" hx-headers='{"Toaster": "true"}''>
    <div class="input-group">
        <label class="label" for="username">Username</label>
        <input placeholder="johndoe" type="text" name="username" id="username" pattern="[a-zA-z0-9À-ž\s]+"
            autocomplete="username" spellcheck="false" autocapitalize="none" maxlength="60" required
            aria-errormessage="username-error" />
        <span id="username-error"></span>
    </div>
    <div class="input-group">
        <div style="justify-content: space-between; flex-wrap: wrap;" class="label-container">
            <label class="label" for="password">Password</label>
            <small><a class="link" hx-target-error="body" href="/auth/forgot-password">Forgot password?</a></small>
        </div>
        <input placeholder="********" type="password" name="password" id="password" spellcheck="false"
            autocapitalize="none" autocomplete="list" minlength="8" maxlength="64" required
            aria-errormessage="password-error" />
        <span id="password-error"></span>
    </div>
    <input type="hidden" name="authenticity_token" value="{{authenticity_token}}" />
    <input aria-hidden="true" name="honeypot" value="" hidden />
    <button data-loading-disable type="submit" class="primary">
        <span data-loading-class="hidden">Sign in</span>
        <span data-loading>Signing in... <span data-loading data-loading-class="spin" class="spinner"></span></span>
    </button>
</form>
<div style="text-align: center;">
    <small>
        Don't have an account?
        <a hx-swap="innerHTML swap:250ms" class="link" hx-target-error="body" hx-target="#form-container"
            href="/auth/register">Sign up</a>
    </small>
</div>