{{#if is_boosted}}
    <head>
        {{> partials/head/main}}
    </head>
    <h1>404 Not Found</h1>
    <p>Sorry, the server is experiencing some issues processing your request.</p>
{{else}}
<!DOCTYPE html>
<html lang="{{ metadata.language }}" data-theme="{{ metadata.theme }}">
    <head>
        <meta name="robots" content="noindex, nofollow">
        {{> partials/head/main}}
        </head>
        <body>
            <main>
                <h1>404 Not Found</h1>
                <p>Sorry, the server is experiencing some issues processing your request.</p>
            </main>
        </body>
    </html>
{{/if}}